\documentclass[zihao=-4,a4paper,fontset=none]{ctexart}
\usepackage[a4paper,margin=2.5cm]{geometry}
\usepackage{amsmath,amssymb}
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{setspace}
\usepackage{indentfirst}
\usepackage[font=small]{caption}
\graphicspath{{figs/}}

% 中文字体（宋体正文，黑体加粗；西文 Times New Roman）
% 宋体无斜体，显式声明 Italic/Slanted 回退到宋体本身，避免 shape undefined 警告
\setCJKmainfont[BoldFont={SimHei},ItalicFont={SimSun},SlantedFont={SimSun}]{SimSun}
\setCJKsansfont{SimHei}
\setCJKmonofont{SimSun}
\setmainfont{Times New Roman}

\setstretch{1.25}
\ctexset{
  section={beforeskip=0.9em,afterskip=0.5em},
  subsection={beforeskip=0.6em,afterskip=0.4em}
}
\allowdisplaybreaks

\begin{document}

% ===================== 标题与摘要 =====================
\begin{center}
{\zihao{3}\bfseries 基于线性规划与对偶分析的微网计划购电策略}
\end{center}

\vspace{0.3em}
\centerline{\textbf{摘\quad 要}}
% TODO 摘要：各问完成后撰写（deAiWriting 摘要三段式）：
% ① 问题锚定：微网光-储-负荷协同、双谷双峰分时电价；② 分问题链路（每问"数据→模型→
% 算法→数值结论"）；③ 验证与特色（三重校验、阈值 1/0.81、规则式对照 +11.71%）
（占位）摘要待问题 2--4 完成后统一撰写。本问已备妥的数值结论：全天购电 59482.7 kWh、购电费 35126.85 元，较无储能运行节省 26.9\%；充放电启停受阈值 $1/0.81\approx1.235$ 支配；规则式策略较线性规划最优贵 11.71\%。

\vspace{0.2em}
\centerline{\textbf{关键词：}\ 微网；分时电价；储能调度；线性规划；对偶分析}

% ===================== 1 问题重述 =====================
\section{问题重述}
% TODO（骨架）：一段背景（微网消纳分布式光伏、外网分时电价、储能低充高放）；
% 一段逐问概述：P1 每天电价负载相同+光伏预测，0:00 制定计划购电策略（供电不低于负载、
% 储能 0:00 与 24:00 同电量），输出表 1/表 2 与 result1.xlsx；P2 实际数据+5 倍紧急购电；
% P3 0/6/12/18 点滚动预报与调整购电（0.5 倍违约、1.5 倍超购）；P4 波动电价重算 P2/P3。

% ===================== 2 问题分析 =====================
\section{问题分析}

问题~1 的电价与小区负载每天相同，光伏发电功率有全天预测，微网要在每天 0:00 一次性给出当天逐 10 min 的计划购电策略，供电不得低于负载，储能设备在 0:00 与 24:00 的储电量相同。电量、负载与光伏三条曲线在一天内峰谷错位，而电能无法大量囤积，跨时段的余缺调剂只能经储能完成；充放效率各为 90\%，1~kWh 电能一进一出只剩 0.81~kWh，低充高放要有净收益，充放两段的电价比须跨过 $1/0.81\approx1.235$ 的门槛，这一权衡贯穿全问。附件~1 的电价呈双谷双峰形态，峰谷价差远超门槛，低充高放有充足的运作空间；首末电量相同叠加曲线逐日重复，全年运行分解为同一个单日问题的逐日重复，只需对 2025 年 1 月 1 日建立单日模型。购电、光伏与储能三路电能流的逐段分配取值连续，目标函数与全部约束均为线性，问题归结为线性规划。求解之外，我们进一步借对偶变量构造节点边际价值，用 KKT 条件证明最优性并解释充放电窗口的启停结构，再对效率与功率上限做灵敏度检验，对偶分析由此成为本文的特色。

% TODO：问题 3/4 完成后，在此各追加一段思路分析（滚动预报+调整结算 / 波动电价重算）。

问题~2 的难点集中在负载与光伏两条曲线的不确定性上：它们随日期变化，而在每天 0:00 规划当日购电时，当日的真实情况尚不可知，能依靠的只有截至前一日的附件~2 历史数据。计划的输入必须先由历史数据预测得到，本问据此拆成前后相接的两个部分。前一部分做预测：单一模型难以在全年不同季节同时保持准确，我们构建机制互补的异构模型池，让不同模型的优势结合、缺点相互抵消，并用指数梯度下降在线更新各成员的权重，使权重向近期误差更小的模型倾斜，对季节切换保有较快的响应。预测的输出不是单点值，而是一组分位数梯度曲线，给后续决策留出保守调整的余地。后一部分做优化：拿到分位数预测后，沿用继承自问题~1 的 LP，补充紧急购电的结算表达式后求解。在这一环节，预测实际上是为优化服务的，分位档调高一些，预估就更保守，5 倍价的紧急购电就更少被触发；调得太低则缺口惩罚失控，调得太高又白白多买，保守换来的罚金节省与过保守付出的购电增量之间存在一个平衡点，我们通过遍历分位档组合、以全年总费用回测的方式把它找出来。求解之外，本文还做了对照实验：把预测环节换成 XGBoost 与 LSTM 两个强大的时序预测模型，计划与结算环节保持不变，比较三组最终的最优成本，以展现本文预测模型的优越性。

% ===================== 3 模型假设 =====================
\section{模型假设}

\begin{enumerate}
\item \textbf{日周期性}：附件~1 的电价与负载曲线逐日相同，光伏预测为同一日曲线，其中电价在 0.3713 与 1.3952 元/kWh 之间，负载在 3309 至 5959~kW 之间，光伏峰值 7612.3~kW，计划得以逐日复制，这是~\ref{sub:build} 节时段划分的依据。
\item \textbf{储能效率口径}：充、放电效率各取 $90\%$，往返效率 $81\%$，损耗只发生在进出储能的路径上，直供负载不打折。设计说明：题面只给"充放电效率为 $90\%$"一个数值，两种记法在文献中都有，文献~\cite{wangxz2017} 对充、放分别取 93\% 与 92\%，文献~\cite{sioshansi2009} 把往返效率记在充电侧；本文取前者，若改按"往返合计 $90\%$"重解，成本为 33416.43 元、低 4.87\%，差异不影响模型结构，声明口径即可。
\item \textbf{功率上限口径}：5000~kW 指储能设备并网侧，即母线端的充、放电功率，两个方向对称；计入效率后电池侧存入不超过 4500~kW、放出不超过 5556~kW，与表~\ref{tab:charge} 的母线计量一致。
\item \textbf{不售电}：购电量与放电量均非负，不向外网反送电；光伏盈余只能充电或弃掉，弃光不计费用，以与题面"购电量"的记账方向一致。
\item \textbf{完全信息}：0:00 制定计划时全天电价、负载与光伏预测已知且无误差，问题~1 为确定性规划，预测误差与缺电惩罚留给问题~2、3。
\end{enumerate}

% ===================== 4 符号说明 =====================
\section{符号说明}

{\small
\begin{center}
\begin{tabular}{ll}
\toprule
符号 & 说明 \\
\midrule
$\Delta t$ & 单时段长度，10 min \\
$\lambda_t$ & 时段 $t$ 的外网电价，元/kWh \\
$L_t$ & 时段 $t$ 的小区负载，kW \\
$P_t^{\mathrm{pv}}$ & 时段 $t$ 的光伏发电预测功率，kW \\
$a_t,\ b_t$ & 时段 $t$ 购电功率中直供部分 / 充电部分，kW \\
$g_t,\ c_t$ & 时段 $t$ 光伏功率中直供部分 / 充电部分，kW \\
$q_t$ & 时段 $t$ 光伏弃光功率，kW \\
$d_t$ & 时段 $t$ 储能放电功率，kW \\
$E_t$ & 时段 $t$ 末储电量，kWh \\
$S_t$ & 时段 $t$ 末的净充电累计量，$E_t=6000+S_t$，kWh \\
$\eta$ & 充放电效率，取 0.9 \\
$E_0,\ E_{\min},\ E_{\max}$ & 储电量初值与界限：6000、1200、10800 kWh \\
$P_{\max}$ & 并网侧充放电功率上限，5000 kW \\
$\gamma_t,\ \sigma_t$ & 节点边际价值 / 边际供电成本，元/kWh \\
\midrule
$\hat L_t,\ \hat P_t$ & 问题~2 中时段 $t$ 经选定分位档修正的负载 / 光伏预测，kW \\
$P_t$ & 问题~2 结算用的光伏实际功率，kW \\
$g_t^{r}$ & 问题~2 结算期实际光伏直供功率，$\min(g_t,\,P_t)$，kW \\
$R_t$ & 问题~2 时段 $t$ 实际总供给，$a_t+g_t^{r}+d_t$，kW \\
$e_t$ & 问题~2 时段 $t$ 供电缺口，kW \\
$E_0^{(d)}$ & 第 $d$ 日 0:00 储电量，滚动取前一日执行期末值，kWh \\
$F^{(d)}$ & 第 $d$ 日计划购电费，元 \\
$w_{t,i}$ & 第 $t$ 日模型 $i$ 的 EGD 集成权重 \\
$\tilde\ell_{t,i}$ & 模型 $i$ 第 $t$ 日的局部核加权损失 \\
$\mu$ & EGD 权重更新学习率，取 0.005 \\
$\alpha$ & 权重重初始化的混合系数 \\
$\tau$ & 决策分位，负载取 $P_{\tau}$、光伏取 $P_{1-\tau}$ \\
$C_u,\ C_o$ & 购电低估 / 高估的单位成本，$5\lambda_t$ / $\lambda_t$ \\
\bottomrule
\end{tabular}
\end{center}
}

\newpage
% ===================== 5 问题1 =====================
\section{问题1模型的建立与求解}

对于电价、负载和光伏发电量等所有数据已知的第一问，我们分析题目和数据信息，建立了简单的LP模型并求解严格最优解。在完成了最优解的结果分析后，我们还通过对偶分析和最优性证明，以及
本场景恰当的灵敏度分析保证了模型的合理和稳健。具体流程如下图所示：

\begin{figure}[htbp]
\centering
\includegraphics[width=0.95\textwidth]{p1_workflow.png}
\caption{问题~1 的建模与求解流程}
\label{fig:p1flow}
\end{figure}

\subsection{LP模型建立}
\label{sub:build}

首先可对附件~1 中的数据做简要预分析。

本题把每日划成 144 个时段 $I_k=[10(k-1),\,10k)$ min，$k=1,\dots,144$。$I_1$ 取附件的第 144 行，该行标记的是次日 0:00 起的 10 分钟，由假设~1 的周期性，其数值与当日 0:00 至 0:10 相同；$I_2$ 至 $I_{144}$ 依次取当天的第 1 至第 143 行。可以简要分析出，电价在深夜与午间各有一个低谷，早 8 时前后与 19 时至 22 时各有一个高峰，双谷双峰的形态给储能低充高放留出了空间。

在本问的微网为小区负载的场景下，构建每个时刻的电能流是理解各购电、充放电和供电的关键所在：

\textbf{外网购电} \ \  作为最基本的电能来源，可以规划从外界直接购入的购电量，并在较长的时间窗口满足外界总购电量略多于总负载的用电量，稍多的部分会在储能设备转化时损耗。
外网购电可能根据存贮计划分流，一部分可转向储电设备并以90\%的效率存储 $b_t$ ，另一部分考虑可以直接转向小区负载供电 $a_t$ 以减少损耗率；

\textbf{光伏发电} \ \  类似的，小区内光伏发电的功率同样进行拆分，拆成直供负载的 $g_t$ 与储能充电 $c_t$ 两路，后者同样存在90\%效率的能量转换。
而由于光伏发电为被动来源，在储能设备几乎存满且负载低的极端情况下，可能出现废弃的光能功率，光伏与储能的协同调度正是处理这类消纳问题的常见框架~\cite{yuanlong2018}。

\textbf{储能供电} \ \  储能设备可以将通过外购和光伏存入的能量再以90\%的效率输出，为小区负载供电。我们记放电总功率为 $d_t$。同时，每个时刻的储电量 $E_t$ 由充放电累计得到

全天购电费为各段电价乘以购电量之和，针对购电成本的目标函数为
\begin{equation}
\min\ F=\sum_{t=1}^{144}\lambda_t\,(a_t+b_t)\,\Delta t .
\label{eq:obj}
\end{equation}

本场景中涉及的基本约束包括：

\textbf{供电平衡}\ \  考虑到微网供给不得低于负载，又由于多供无益。在所有时刻维持总供电刚好满足需求负载，取等式
\begin{equation}
a_t+g_t+d_t=L_t .
\label{eq:load}
\end{equation}

\textbf{光伏电量平衡}\ \  根据电能流的梳理，光伏发电分流到直供负载、储电以及极端情况的废弃量，即
\begin{equation}
g_t+c_t+q_t=P_t^{\mathrm{pv}} .
\label{eq:pv}
\end{equation}

\textbf{储能动态}\ \  对于储能设备而言，每一个时刻的储电量变化需考虑所有充放电路径。由于其充放电效率为 90\% ，迭代时注意存在损耗，即
\begin{equation}
E_t=E_{t-1}+0.9\,(b_t+c_t)\,\Delta t-\frac{d_t}{0.9}\,\Delta t ,
\label{eq:soc}
\end{equation}

\textbf{储电量约束}\ \  为保护设备在长期运转的健康水平，每个时刻的储电量需要避免过高或过低
\begin{equation}
1200\le E_t\le 10800
\label{eq:ebnd}
\end{equation}
同时，充放电在单位时间的功率也存在界限，采用行业常用的并网侧约束，即损耗之前进行约束  $b_t+c_t\le 5000$、$d_t\le 5000$~kW 。

\textbf{周期边界条件}\ \  场景已经给定 2025 年 1 月 1 日 0:00 的储电量为 6000~kWh，且要求每天储能设备在 0:00 与 24:00 的储电量相同，故每晚午夜储电量均为 6000~kWh，计划逐日执行时第 $k$ 天 0:00 的节点 $E_{144k}$ 都取该值；罐内储电量本身仍随充放电在界限内波动，该要求在单日模型中表现为首末两端
\begin{equation}
E_0=E_{144}=6000\ \mathrm{kWh} .
\label{eq:bd}
\end{equation}
其中，变量非负隐含 $d_t\le L_t$，放电不会超过负载，不发生向外的反送。

合并式~\eqref{eq:obj}至式~\eqref{eq:bd}，问题一的完整LP为:
\begin{equation}
\begin{aligned}
\min\quad & F=\sum_{t=1}^{144}\lambda_t\,(a_t+b_t)\,\Delta t \\
\text{s.t.}\quad
& a_t+g_t+d_t=L_t,\qquad g_t+c_t+q_t=P_t^{\mathrm{pv}}, \\
& E_t=E_{t-1}+0.9\,(b_t+c_t)\,\Delta t-\frac{d_t}{0.9}\,\Delta t,\\
& 1200\le E_t\le 10800,\qquad b_t+c_t\le 5000,\qquad d_t\le 5000,\\
& E_0=E_{144}=6000,\qquad a_t,\,b_t,\,g_t,\,c_t,\,d_t,\,q_t\ge 0,\\
& t=1,\dots,144 .
\end{aligned}
\label{eq:full}
\end{equation}

\subsection{LP模型求解}
\label{sub:solve}

式~\eqref{eq:soc} 中的储电量 $E_t$ 并非独立的决策量，任一时刻的储电量都由之前的充放电决策唯一确定，求解时把它消去，改记业内常见的净充电累计量 SOC
\begin{equation}
S_t=\sum_{s\le t}\Bigl[\,0.9\,(b_s+c_s)-d_s/0.9\,\Bigr]\Delta t ,
\label{eq:cum}
\end{equation}
以 $E_t=6000+S_t$ 代入，容量界限与终端条件都化为对 $S_t$ 的直接约束。这一消元可提前省去 144 个状态变量及其对应的 144 条递推等式。虽然$S_t$ 的界限行带前缀求和，
非零元随时段增多，约束矩阵变得更稠密，但这在 144 段的规模下不构成负担。

最终交给求解器的是 864 个决策量、289 个等式约束与 576 个不等式约束。
在实际求解中，容量界限行与终端归零行的对偶变量合成了各时点的节点边际价值，~\ref{sub:dual} 节的 $\gamma$ 递推将直接构造自这组输出。求解调用 scipy.optimize.linprog 接口
的 HiGHS 求解器，容差沿用默认设置，单次求解约 0.25~s。

求解器在返回最优解的同时给出全部对偶变量，~\ref{sub:dual} 节的影子价格直接取自这一输出；
求解前后还通过了以下三类自检：

\textbf{全天能量守恒}\ \ 购电量加光伏发电量应等于负载、弃光、充电损耗与放电损耗之和，两侧相差 $1.5\times10^{-11}$~kWh，即恒等式累积误差完全为运算精度误差；

\textbf{购电策略合理}\ \ 最优的购电策略应该是利用本题数据的双谷双峰形态，在低电价时购入并存储并在高电价时取用，这导致解的外网购电存在分块特征。
具体分析见~\ref{sub:results} 节的表~\ref{tab:charge} 与图~\ref{fig:dispatch}；

\textbf{无同时充放电记录}\ \ 同时充放电这一理论上绝非最优的策略在解中并未出现。从实际情况考虑，对储能设备同时充放电更易由于总功率较高而造成损伤；同时从优化模型上考虑，发生同时充放电时将其相互抵消，即可省下存储 90\% 所带来的能量损失，这在理论上可降低成本，严格的论证见~\ref{sub:dual} 节。
\subsection{模型结果分析}
\label{sub:results}

全天购电 59482.7~kWh、购电费 35126.85 元；无储能运行时按逐段缺口 $\max(0,\,L_t-P_t^{\mathrm{pv}})\Delta t$ 购电需 48052.05 元，储能低充高放节省 12925.20 元、降幅 26.9\%，数据见表~\ref{tab:buy}。
六个指定时段里，10:00-10:10、14:00-14:10 与 20:00-20:10 的购电量均为0，这些时刻光伏加储能足以覆盖负载。

计划购电量按模板逐行填写，第 2 至第 144 行依次对应 $I_2$ 至 $I_{144}$，末行填 $I_1$ 的数值，即次日首段的计划。

\begin{table}[htbp]
\centering
\caption{微网指定时间段微网购电量及全天购电情况}
{\small
\begin{tabular}{cccc}
\toprule
时间段 & 购电量/kWh & 时间段 & 购电量/kWh \\
\midrule
10:00-10:10 & 0.00 & 16:00-16:10 & 394.93 \\
12:00-12:10 & 486.40 & 18:00-18:10 & 636.98 \\
14:00-14:10 & 0.00 & 20:00-20:10 & 0.00 \\
\midrule
全天购电量 & 59482.7 & 全天购电费/元 & 35126.85 \\
\bottomrule
\end{tabular}}
\label{tab:buy}
\end{table}

表~\ref{tab:charge} 按四小时块汇总，汇总块来自 $I_{24k+1}$ 至 $I_{24k+24}$ 逐段统计。

表中可见，充电集中在 0:00-4:00、12:00-16:00 与 20:00-24:00 三个块，放电主力在 4:00-8:00 与 16:00-20:00；
这一特征结果高度符合数据分析中描述的峰谷特征，该策略为LP的核心特征。
\begin{table}[htbp]
\centering
\caption{储能设备在指定时间段的充放电量及 0:00 和 24:00 的储电量}
{\small
\begin{tabular}{ccc}
\toprule
时间段 & 充电量/kWh & 放电量/kWh \\
\midrule
0:00-4:00 & 4500.00 & 0.00 \\
4:00-8:00 & 833.33 & 5947.42 \\
8:00-12:00 & 3954.63 & 2121.42 \\
12:00-16:00 & 6119.37 & 91.10 \\
16:00-20:00 & 0.00 & 5068.69 \\
20:00-24:00 & 5333.33 & 3571.31 \\
\midrule
0:00 与 24:00 储电量 & \multicolumn{2}{c}{6000} \\
\bottomrule
\end{tabular}}
\label{tab:charge}
\end{table}

下图给出了储能设备的储电量变动情况与各时段功率。
日循环呈两充两放结构，0:00-1:00 以 5000~kW 顶格充电 4500~kWh，5:40-5:50 抓住全日最低价 0.3713 元/kWh 补充 833.3~kWh，
储电量触及上限 10800~kWh；早峰 6:00-9:40 放电 8068.8~kWh，恰好覆盖全部负载，这三个多小时内购电为零；午间由光伏盈余充电并在低价段补购；
晚峰 18:50-20:50 放电 8530.7~kWh，电价最高的 1.3952 元/kWh 时段购电为零；22 时起分批回充，24:00 回到 6000~kWh 闭环。特别的，外购充电全部落在 0.371 至 0.479 元/kWh
 的时段里，放电时段的电价最低也有 0.690 元/kWh；另外，
 全天弃光为零，光伏发电量被负载与储能完全使用。

\begin{figure}[htbp]
\centering
\includegraphics[width=0.92\textwidth]{fig2_dispatch.png}
\caption{解的储能状态与各时段购电、充电及放电功率}
\label{fig:dispatch}
\end{figure}

\subsection{对偶分析及最优性证明}
\label{sub:dual}

LP 的对偶变量给出比数值解更深一层的解释。我们给各类约束挂上影子价格，据此定义节点边际价值 $\gamma_t$：在时段 $t$ 末白得 1~kWh 储能，全天购电费能省下多少。$\gamma$ 沿时间轴满足递推
\begin{equation}
\gamma_t=\gamma_{t+1}-\eta_t+\theta_t ,
\label{eq:gamma}
\end{equation}
其中 $\eta_t$、$\theta_t$ 是储电量上、下限约束的对偶变量。内点节点上 $\gamma$ 逐段不变，能量躺在罐里不生息；满罐节点 $\gamma$ 下移 $\eta_t$，空罐节点 $\gamma$ 上移 $\theta_t$。由 KKT 互补松弛条件可得充放电的三条启停判据，对偶理论见文献~\cite{boyd2004}。充电段满足 $\lambda_t=0.9\gamma_t$，功率顶格段另加正的影子溢价；放电段满足 $\sigma_t=\gamma_t/0.9$，$\sigma_t$ 为该段边际供电成本；空闲段的价格落在 $0.9\gamma_t$ 与 $\gamma_t/0.9$ 之间的迟滞带内。把充、放电段的等式联立，可得跨窗口搬运能量的门槛，放电价与充电价之比须不低于
\begin{equation}
\frac{1}{0.81}\approx 1.235 .
\label{eq:th}
\end{equation}
说白了，价差比不到 1.235 的两段之间，LP 一千瓦时也不会搬。同一段内边充边放的情形可以先用损耗的账排除。取充分小的 $\delta$，若某段同时有 $b_t>0$ 与 $d_t>0$，把充电减 $\delta$、放电减 $0.81\delta$、直购增 $0.81\delta$，储电量轨迹逐点不变，购电费净省 $0.19\,\lambda_t\,\delta\,\Delta t$，只要电价为正，原解就还不是最优。对偶条件给出同一结论，边充边放要求 $\lambda_t=0.9\gamma_t$ 与 $\lambda_t=\gamma_t/0.9$ 同时成立，推出 $\gamma_t=0$ 且 $\lambda_t=0$，与恒正的电价矛盾。故最优解中不存在同段充放，无需为排除该行为引入 0-1 变量，这与文献~\cite{wangxz2017} 用状态变量处理储能充放互斥的做法形成对照。

图~\ref{fig:shadow} 画出 $\gamma$ 与 $\lambda$ 的全天对比，底色可区分出充放电窗口，有以下两处结构值得讨论。

首先是，两个放电大窗口 6:00-9:40 与 18:50-20:50 内 $\sigma$ 分别为 0.591 与 1.256 元/kWh，都低于当时的电价，储能把两个价格峰完全削平，窗口内购电为零。

其次是，15:10-18:30 储电量顶在 10800~kWh 的平台期，$\gamma$ 从 0.733 爬升至 1.130 元/kWh，每经过一个满罐节点上移一点，罐容稀缺的影子价随放电临近在累积。最优性方面，互补松弛条件在全时段的违约不超过 $1.4\times10^{-17}$ 元/kWh，节点价值的有限差分重解与对偶值偏差为 $1.2\times10^{-12}$，两条独立途径相互印证。

\begin{figure}[htbp]
\centering
\includegraphics[width=0.85\textwidth]{fig3_shadow.png}
\caption{储能节点边际价值 $\gamma$ 与电价 $\lambda$ 的全天对比}
\label{fig:shadow}
\end{figure}

\subsection{模型灵敏度分析}
除了对本LP结果的理论分析，还可以从灵敏度进行评价。

本问选择了$\eta$,$P_{\max}$这两个行业内通常不固定的指标，尝试进行灵敏度分析。虽然本题给定了具体数值，但我们认为如果在给定值的邻域波动参数也能通过灵敏度检验，则
可以较好的说明模型稳健性；其次，我们选择了问题在业界的常见方案做结果的对比，以体现模型的优越性。

\label{sub:eval}

\textbf{充放电效率 $\eta$,} \ \  本题在储能设备参数处给定 $\eta = 90\%$, 但是在实际生产中同一批储能设备的参数都可能存在误差，且在使用、老化的过程中，电机的效率损失几乎是必然的
；此外，该参数在模型的求解过程中及其重要。 由于$\eta$ 决定套利阈值 $1/\eta^2$，效率越低，值得搬运能量的价差组合越少，解的结构就会随之改变。

所以本文围绕90\%, 扫描取 0.85 至 0.95 共五档，每档重新求解完整的规划，结果见表~\ref{tab:sens}。
成本随 $\eta$ 单调下降，每提高 0.01 仅约降 0.8\%，并未出现较大敏感波动。

失效边界在 $\eta\approx0.841$。早峰 6:00-9:40 需要储能送出 8068.8~kWh，满电自 10800~kWh 放至下限 1200~kWh 最多送出 $9600\,\eta$~kWh，
$\eta$ 低于 $8068.8/9600\approx0.841$ 后单个日循环放不满早峰缺口，必须在午间以更高的电价二次回充，成本曲线在此转陡。

\begin{table}[htbp]
\centering
\caption{充放电效率 $\eta$ 的敏感性扫描}
{\small
\begin{tabular}{cccc}
\toprule
$\eta$ & 阈值 $1/\eta^2$ & 购电费/元 & 较 $\eta=0.90$ \\
\midrule
0.85 & 1.384 & 36603.21 & $+4.20\%$ \\
0.88 & 1.291 & 35696.82 & $+1.62\%$ \\
0.90 & 1.235 & 35126.85 & 题面取值 \\
0.92 & 1.182 & 34575.53 & $-1.57\%$ \\
0.95 & 1.108 & 33767.02 & $-3.87\%$ \\
\bottomrule
\end{tabular}}
\label{tab:sens}
\end{table}

\textbf{功率上限 $P_{\max}$} \ \  分析可得，解中有 19 个时段的充电功率顶在 5000~kW，该约束频繁激活，值得单独查验。
把 $P_{\max}$ 放宽到 6000~kW 重解，成本仅降 0.12\%。其看可能的原因是低价时段的总电量足以把储能设备充满，上限改变的只是充电在时段之间的分布，
而不是充入的总量；放电峰值功率 4294~kW 从未触顶，两个方向中只有充电侧受到约束。而假设~3 中我们对于$P_{\max}$的并网侧理解与“电池侧”，认为时电池实际存入或输出的功率可达 $P_{\max}$，对结果的影响同样被限制在这一量级。

% ===================== 6 问题2 =====================
\section{问题2模型的建立与求解}

\label{sub:p2frame}

问题~2 继承了问题~1 构建的LP模型，但是有三个条件改动。

\textbf{日前预测必要性}\ \ 负载与光伏逐日变化，考虑当日真实值在制定计划时不可知，这里便衍生出了基于日前信息进行时间序列预测的需求。同时，
考虑到负载和光伏发电数据的波动特征等可能存在差异，在建模时需要通过不同的方法学习不同特征。

\textbf{紧急购电}\ \ 由于计划依据的数据来源于预测，执行过程中因误差导致的若供电不足，须按交易时刻电价的 5 倍向外网紧急购电。

\textbf{取消日起储能限制}\ \ 当第二问不再要求每天24:00匹配00:00的6000kWh电量，跨度为每日的LP模型一定会在午夜之前将储能电量全部放完以
最小化当日成本，但这在长期看来是绝对不可取的。午夜之前的低价时段不外购充电，反而放电，该策略是完全不符微网场景逻辑的。
但由于每天的预测和计划独立，不能直接将LP的时间跨度提到一年。故，文本将储电量根据当前电价，作为减项折算到成本中。这可以从LP根本上杜绝其自发在午夜前放电的行为。

据此，本问的求解框架分为日前预测部分及LP求解部分，具体流程如下图。

\begin{figure}[htbp]
\centering
\includegraphics[width=0.95\textwidth]{p2_workflow.png}
\caption{问题~2 的建模与求解流程}
\label{fig:p2flow}
\end{figure}

\subsection{日前预测模型的建立}
\label{sub:p2fbuild}

预测层需在每日0:00时刻，利用截止前一日的全部历史数据，对当日144个10分钟间隔时点的小区负载功率与光伏发电实际功率进行日前预测。同一天
预测值不考虑带回增量训练以避免误差累积。

考虑到从31天数据的冷启动到后期数百天的数据跨度，以及季节性的峰值等特征可能漂移，我们认为预测层实际的关键在于
在不同数据量情况、两种不同预测目标特征、季节性趋势明显、特殊的噪声和信号识别上的高自适应能力，该模型同时还需要兼顾短时天气变化的快速响应。
可见，这种多环境的自适应需求很难由单ML模型满足，故我们拟采用多模型互补的思路，通过高度灵活的方法调整模型权重，尝试达到在不同场景的高预测性能。

\subsubsection{两目标时序特性分析}

在多模型的组合构建前，需要分别分析小区负载和光伏发电量的时序特性，方便后续进行更加具备科学性的模型选择。纳入不足或者偏向明显的组合，可能导致
模型池不擅长一些情境的信息捕捉；而纳入过多各类模型，会使得求解效率低下，而同类模型特性互相叠加影响，类似共线性的效应严重影响求解效率。

\textbf{小区负载}\ \ 对其自相关分析，结果显示滞后7阶自相关系数达0.971，而滞后1阶仅0.373，这表明周周期为首要周期成分。
此外观察到周内模式呈现显著分化，周一至周四及周日日均负荷约5165 kW，但周五、周六骤降37\%至3265 kW，反映了工作日与周末的居民用电行为差异，
符合生活常识。
而在日级别方差分解中，周季节成分占89.3\%，趋势占14.1\%，残差仅4.4\%，进一步验证了周周期的主导地位。
日内峰谷比在[1.80,2.11]中，峰值时点存在季节漂移，由1月09:00对应早高峰的峰值，7月漂移至17:30对应晚高峰，月度均值极值比为1.23。

\textbf{光伏发电}\ \ 类似的，滞后1阶自相关系数为0.956，而滞后7阶为0.878，表明日连续性远强于周周期，不同于负载特征。
同时，工作日与周末的表现无显著差异，符合自然特征。月度均值极值比达1.93，这由气候缘故决定。
且光伏发电的物理约束明确，即夜间功率恒为0、峰值恒在12:00前后，日出日落时点由天文规律确定。故，天气是主要噪声来源，且日间变异系数在区间[0.06,0.11]。

上述特性直接驱动了模型选型策略。负载需重点建模周周期与季节漂移，光伏需重点建模日连续与年周期，并利用物理约束强制夜间置零。
考虑到两个序列的显著时序特性差异，采用独立模型组合而非统一模型池。

\subsubsection{异构模型池构建}

针对"单一模型难以覆盖全年周期特性"的问题，本文构建机制差异化的异构模型池，而非依赖单一算法。模型选型遵循三条原则：（1）每个模型捕捉不同的时序成分（周期/趋势/曲线形态/相似日/非线性）；（2）剔除误差高度相关的冗余模型；（3）所有模型均在滚动设定下合规运行（不使用未来信息）。

模型池的构建经历了实证精简过程：初始纳入7个负载模型，经全量消融实验（见\ref{sub:p2fsolve} 节）发现函数型主成分分析（FPCA，全期MAE 258.32）和谐波回归（HarmonicReg，全期MAE 353.82）的边际贡献为负，移除后集成MAE反而略降0.2~0.4，夏季6-7月改善1~3\%。因此最终负载池精简为5个模型，光伏池保留5个模型。

\textbf{负载模型池}


（1）季节朴素预测：预测值等于上周同一时点的实际值。
作为周周期强基准，其全期MAE为176.80。该方法无需训练，在周周期稳定的春秋季表现优异。

（2）月度均值预测：近4周同一时点的算术平均。借鉴TVJMA（Time-Varying Jackknife Model Averaging）的局部窗思想，对短期波动起平滑作用。全期MAE为188.88。

（3）STL分量预测：对每个时点的历史日序列做STL分解（period=7, seasonal=7, trend=15, robust=True），将序列分解为趋势、季节与残差三部分；趋势分量采用阻尼系数0.8线性外推（抑制趋势突变），季节分量取上一周同相位值，残差置零。STL原文（Cleveland, 1990）的鲁棒局部加权回归可有效抵御异常值。全期MAE为178.59。

\textbf{方法取舍}\ \ 选择STL而非CEEMDAN/VMD等模态分解方法，原因是后者需在全局数据集上分解，在滚动设定下会引入未来信息泄漏；STL仅在训练窗内分解，完全合规。

（4）kNN相似日：以昨日完整曲线为查询，在近60天历史中寻找归一化欧氏距离最近的3个候选日（负载限定同星期几），以距离倒数为权重取候选日次日曲线的加权平均。该方法借鉴TESLA（Akyurek等，UCSD）在光伏预测中的相似日框架，文献报告其RMSE较persistence降低50\%。全期MAE为188.80。

（5）LightGBM：自XGBoost改进的梯度提升树，工程上极常用的机器学习模型。
构建18维特征（日历特征：doy, dow, month, weekend, hour, sin\_t, cos\_t, sin\_dow, cos\_dow,
time\_idx；滞后特征：lag1, lag2, lag7, lag14, lag21, lag28, lag7mean, lag1dmean）。
强正则配置：n\_estimators=600, learning\_rate=0.03, num\_leaves=15, min\_child\_samples=30,
subsample=0.7, colsample\_bytree=0.7, reg\_alpha=1.0, reg\_lambda=5.0，早停轮数50。全期MAE为172.71，为负载最优单模型。


\textbf{光伏模型池}


（1）Persistence：昨日同刻值。本文选择该极简方法，而非在短期做指数平滑或小窗口去噪，是因为希望赋予
模型池对于每日的突发气候变化以最快速的响应能力。光伏日形状相对稳定时的强基准，全期MAE为185.35。

（2）kNN相似日：同负载kNN逻辑，但不限定星期几（光伏工作日/周末无差异），全期MAE为205.04。

（3）STL分量预测：同负载STL逻辑，全期MAE为212.73。

（4）FPCA（函数型主成分分析）：近14天曲线SVD低秩重构，采用阻尼score persistence（预测曲线=均值+0.4×最近一日主成分得分重构），在保留近期天气信息的同时抑制异常传播。预测值clip为非负。全期MAE为160.71，为光伏最优单模型。该方法借鉴Wagner-Muns等（2018, IEEE T-ITS）在交通流量15分钟粒度预测中的FPCA应用。

（5）LightGBM：同负载特征工程，额外强制夜间（hour≤6或hour≥20）预测置零。全期MAE为192.14。

\subsubsection{EGD时变权重集成}

在完成了两预测指标的模型池搭建后，需要为池中的各子模型进行合理的加权。
若仅通过简单平均或用全期误差定一组固定权重，本题数据的季节性和及时响应的灵活需求很难被满足；而若通过一些能力强大的ML方法，
如通过一个 LightGBM 根据全期误差进行全年度的赋权，但由于在夏季空调负荷上的价值和在秋季平稳周期上的价值并不相同，
全期误差是这两段表现的平均，平均之后无法对不同阶段的预测产生差异化策略，平均的误差会同时拉低两季的预测表现。

故，我们考虑
动态地为二者进行赋权，确保在所有场景均
具备优秀的自适应性以机动调整预测倾向。这会使得权重逐日在线更新，把当天的模型池内策略完全由近期数据驱动。

\textbf{指数梯度下降的权重更新}\ \ 记第 $t$ 日模型 $i$ 的权重为 $w_{t,i}$，当日预测误差结算完毕后按指数梯度下降更新
\begin{equation}
w_{t+1,i}=\frac{w_{t,i}\,\exp(-\mu\,\tilde\ell_{t,i})}{\sum_j w_{t,j}\,\exp(-\mu\,\tilde\ell_{t,j})},
\label{eq:egd}
\end{equation}
其中 $\tilde\ell_{t,i}$ 为模型 $i$ 的局部核加权损失，学习率 $\mu=0.005$。

上式的逻辑和效果直白，即指数地压低当日误差大的成员，使误差小的成员份额上升，并将分母归一化以保证权重始终构成一个概率分布。
其中，学习率取得较小，以保证让权重随时间的漂移速度不能过快，避免某一日的偶然大误差把某个成员一脚踢出。

该时变权重集成的框架来自于在线学习里 Hedge 算法的指数加权策略~\cite{zhang2023onenet}，且其已经被
证明具有最优 regret 界~\cite{mourtada2019}。在该框架下，累积损失至多比事后最优的成员
多出 $O(\sqrt{T\log N})$ 量级。
所以，即使伴随季节切换及其引发的权重迁移，EGD时变赋权下的总损失与日前已知当日数据的理想情形，
只存在一个随天数缓慢增长的小量差距，这对赋权
提供了极佳的稳健性支撑。

\textbf{损失的局部核加权}\ \ 考虑到单日误差的噪声有较大波动，直接拿当日损失更新权重可能引发权重随天气等波动特征产生严重波动。
故采用EGD将最近 30 天的窗口内对历史损失做指数衰减加权
\begin{equation}
\tilde\ell_{t,i}=\frac{\sum_{s=t-29}^{t}0.95^{\,t-s}\,\ell_{s,i}}{\sum_{s=t-29}^{t}0.95^{\,t-s}},
\qquad \ell_{s,i}=\mathrm{MAE}\bigl(y_s,\ \hat y_{s,i}\bigr),
\label{eq:kernel}
\end{equation}
其中，衰减系数取 0.95 会导致当天的权重约为 30 天前的四倍。这可以让模型池更倾向通过近期的场景特征来选择赋权策略，同时也保留稍远时点，如一两周前的影响。
这一局部窗加权的思路取自时变模型平均 TVJMA~\cite{sun2021tvjma}，它用每个时点附近的局部信息挑选合成权重，而不是在全样本上定一套固定权重，
与我们逐日更新的需求达成好匹配。

在算出日均损失后对每日做归一化，以避免在光伏误差整体偏大的阴雨月份，
损失的量级主导权重更新，从而直接覆盖模型池成员之间的相对优劣。

\textbf{周期性的重初始化}\ \ 这一套EGD持续更新权重也存在代价，权重可能会停留在旧模式里，无法做出积极的策略变化。
若某个成员模型在夏季被压到很低的份额，此后即便秋天的数据重新对它有利，指数更新也较缓慢的只能把它往回拉，这不能满足一些情况下的快速响应需求。
为此，我们每隔 7 天将模型重训：
\begin{equation}
w_t\leftarrow \alpha\,w_{\mathrm{init}}+(1-\alpha)\,w_t,\qquad \alpha=\max\Bigl(0.05,\ 0.5\,\exp\bigl(-(N-35)/60\bigr)\Bigr),
\label{eq:reinit}
\end{equation}
其中， $N$ 为已训练天数。在最初的冷启动阶段，由于数据太少$N<35$ ， $\alpha$ 偏大
权重被拉回一个偏向简单方法的起点；而随着数据量的增加啊，后续的 $\alpha$ 衰减到 0.05 的下限，
只保留一点微弱的复位，不再打扰已经学稳的权重。即长线的在线更新负责跟踪学习，周期性的复位负责切换\cite{zhang2023onenet}

\textbf{冷启动权重与滚动训练}\ \ 在初始训练时只有 31 天数据，机器学习成员模型学习效果不佳，
而简单平均在高不确定性下反而更稳~\cite{graefe2015}。故据此让初始权重偏向简单方法，具体冷启动结果见下表。

TODO
表负载侧取季节朴素法 0.25、
历史均值 0.20、STL 0.20、kNN 0.20、LightGBM 0.15，光伏侧取 Persistence 0.30、kNN 0.20、STL 0.15、
FPCA 0.15、LightGBM 0.20。

结合上文周期性重初始化考量，将LightGBM 每 7 天重训一次，与上面的权重复位同步；
而在训练过程中我们不考虑将训练出来的当日前时间预测值作为新输入，增量训练并预测当日后时间的目标。这会极大程度上扩大噪声，尤其在本题时间点分段较多的场景下。
\subsubsection{预测模型检验}
\label{sub:p2fsolve}

训练数据取自附件~2 全年 365 天、每天 144 个时点的记录，2025 年 1 月的 31 天作初始训练数据，
滚动预测点逐天预测。

对于性能的评价，本文取指标 MAE、RMSE 与 MAPE，光伏另计仅统计 06:00--20:00 的白天 MAPE，排除夜间零值对百分比的失真。

同时，我们还选取了两模型池中涉及的所有单个模型进行指标对比，以证明模型池的优异表现并不是单个强大模型主导的，而是不同特征模型的搭配互补。

\textbf{集成优于单模型}\ \ 这一验证使模型池方法涉及的核心。
从表~\ref{tab:p2fsingle} 中可以分析出，对池内成员，
负载集成 162.99 把最优单模型 LightGBM 的 172.71 再降 9.72，优于全部成员，
光伏集成 165.96 虽高于最优单模型 FPCA 的 160.71，但优于其余全部成员。
光伏白天 MAPE 由 7.83\% 升至 8.85\%，是 5~6 月阴雨月份误差结构变化所致，全期 MAE 仍明显占优。

\begin{table}[htbp]
\centering
\caption{池内单模型与 EGD 集成的全期 MAE}
{\small
\begin{tabular}{lclc}
\toprule
负载模型 & MAE & 光伏模型 & MAE \\
\midrule
LightGBM & 172.71 & FPCA & 160.71 \\
SeasonalNaive & 176.80 & Persistence & 185.35 \\
STL & 178.59 & LightGBM & 192.14 \\
kNN & 188.80 & kNN & 205.04 \\
HistMean & 188.88 & STL & 212.73 \\
\midrule
EGD 集成 & 162.99 & EGD 集成 & 165.96 \\
\bottomrule
\end{tabular}}
\label{tab:p2fsingle}
\end{table}

\textbf{LSTM对比}\ \ 为更加直接的表名本日前预测模型的优越性，我们直接选用了工程上的常用ML模型LSTM，对相同的数据集进行训练。

其训练结果表现明显更差，负载 MAE 为 682.1，为本模型的 4.2 倍，光伏MAE 443.4为 2.7 倍，
在 6~7 月与 10~11 月的季节变更厂家下MAE也出现了明显的短期激增。
其可能问题在于纯序列缺少星期几、季节这类日历特征，即使LSTM拥有长期记忆识别和短期预测的优秀能力，其固定的训练窗口
也跟不上季节模式的切换。而本文特征工程后基于EGD加权的模型池，在小数据与一些信息漂移的场景里反而稳健。

\textbf{消融实验}\ \ 通过控制变量下除去特定模型，可以直观反映池内五个模型成员是否都有存在理由。

逐一移除成员并重跑 EGD 权重更新，结果见表~\ref{tab:p2fablate}。负载池的答案是 STL 与 LightGBM 不可或缺，移除后 MAE 分别上升 4.75 与 2.52；季节朴素法与历史均值移除后集成反而略优 0.13 与 0.35，但二者是周周期的强基准与对照，保留的代价不足 0.4。光伏池中 FPCA 一家独大，移除后 MAE 上升 18.56，其余成员的贡献都不超过 1：光伏各模型间的误差相关性高达 0.984~0.998，天气噪声主导下所有模型在同样的日子犯错，其余成员的价值在于 FPCA 失效的极端天气日提供修正。负载池初始 7 模型剔除 FPCA 与谐波回归的精简决策，同样出自这组数据。

\begin{table}[htbp]
\centering
\caption{预测模型池消融实验（MAE 单位 kW，变化为相对完整模型的差值）}
{\small
\begin{tabular}{llcllc}
\toprule
负载消融方案 & 负载MAE & 变化 & 光伏消融方案 & 光伏MAE & 变化 \\
\midrule
完整 5 模型 & 162.82 & 基准 & 完整 5 模型 & 159.77 & 基准 \\
去掉 STL & 167.57 & $+4.75$ & 去掉 FPCA & 178.33 & $+18.56$ \\
去掉 LightGBM & 165.34 & $+2.52$ & 去掉 Persistence & 160.55 & $+0.78$ \\
去掉 kNN & 163.00 & $+0.18$ & 去掉 STL & 160.32 & $+0.55$ \\
去掉 SeasonalNaive & 162.69 & $-0.13$ & 去掉 kNN & 159.71 & $-0.06$ \\
去掉 HistMean & 162.47 & $-0.35$ & 去掉 LightGBM & 159.60 & $-0.17$ \\
\bottomrule
\end{tabular}}
\label{tab:p2fablate}
\end{table}
\textbf{响应月度及季节检验}
权重能否跟随季节，月度误差先给出了漂移存在的证据。负载 MAE 在 6、7、9 月达 208.5、272.9 与 256.7，对应夏季空调高峰与季节转换期，4 月、11 月仅 97.4 与 106.6，对应模式稳定的春秋季；光伏在 5~6 月阴雨期达 201.9 与 236.7，12 月天气稳定且功率本身低，最低至 82.6。面对这样的起伏，EGD 权重的轨迹显示模型确实在换挡，负载 LightGBM 权重夏季升至 0.80~0.83 抓住空调负荷的非线性，秋季自动回落、由权重 0.41 的 STL 与 0.38 的季节朴素法接管，光伏 FPCA 全年 0.89~0.99 稳定主导。整个切换由式~\eqref{eq:egd} 自动完成，无需人工干预，这正是选择在线权重而非固定权重的理由。

\textbf{过拟合检验}\ \ 虽然前文与LSTM对比更优，但是还需要通过过拟合检验，才能说明本模型并不是学到了噪声才有较高的性能指标。

我们采用用时序切分查验来判断机器学习成员是否记住了训练噪声，前 70\% 训练、后 30\% 测试。
负载训练与测试 MAE 为 261.6 对 321.3，比值 1.23，光伏为 190.0 对 207.6，比值 1.09；
不加正则的历史配置过拟合比达 4.83，强正则加 50 轮早停把它压低约 75\%。
其余成员为无参数或低参数的统计模型，不存在此风险。


\subsection{考虑紧急购电的 LP 模型}
\label{sub:p2lp}

\subsubsection{分位数预测的必要性与决策分位}
\label{sub:p2qneed}

6.1 节的 EGD 集成预测对每个时点输出对应的预测值。题面对两类预测误差的计价并不对称：某时段少购 1~kWh，供电不足的缺口须按交易时刻电价的 5 倍向外网紧急购电；而多购电仍按原价结算，且多余的购电或光伏还能充入储能，留待之后的时段使用。因此计划层应当适度偏多购电。若直接把这份集成预测输入计划层，相当于用同一标准处理低估和高估，容易低估缺口的经济损失。模型池组的集成预测直接进入计划层时，全年 48096 个时段中有 25466 个时段出现缺口，紧急购电费为 413.39 万元。

分位调整只作用于计划层的输入，不改变 6.1 的预测本身。EGD 集成仍然给出负荷和光伏的基本走势，对每个时点，修正量取该时点过去 30 天预测残差的经验分位数，修正后的预测为
\begin{equation}
\hat y_{t}^{(q)}=\hat y_t+Q_q\bigl(\{y_s-\hat y_s\}_{s=t-30}^{t-1}\bigr),
\label{eq:qra}
\end{equation}
其中 $Q_q$ 为该时点残差样本的 $q$ 分位数。由于不同购电计价的不对等，负荷向上取较高分位以减少低估，光伏向下取较低分位以减少高估。

曲线的实际分位与名义分位存在偏差：名义 80\% 的中间区间在负荷和光伏上的实际覆盖率为 75.0\% 和 74.3\%，名义 16.7\% 的上侧分位实际被超过 19.3\% 和 19.1\%，残差分位整体偏激进，因此保守修正之后仍会留下缺口。

分位数越高，负荷侧预测通常越大、光伏侧预测通常越小，计划购电费随之增加，紧急购电费则下降，二者之间存在费用平衡点。设低估 1~kWh 的边际代价为 $C_u=5\lambda_t$，高估 1~kWh 的边际代价为 $C_o=\lambda_t$，代价比对应的参考分位为
\begin{equation}
q^{\mathrm{ref}}=\frac{C_u}{C_u+C_o}=\frac{5\lambda_t}{5\lambda_t+\lambda_t}=\frac{5}{6}\approx 0.833.
\label{eq:newsvendor}
\end{equation}

该式只计入单个时段的购电代价，没有包含储能跨时段传递、光伏弃电、SOC 上下限与日末存量价值，因此不能直接当作全年费用最优值。式~\eqref{eq:qra} 在任意分位处都可取值，分位可以连续搜索，不必绑定到某几个固定档位，分位档的最终取值因此由全年费用回测确定：令负载取 $P_{\tau}$、光伏取 $P_{1-\tau}$，对每个候选 $\tau$ 重新执行 334 天计划求解与实际数据结算。以 0.833 为中心、步长 0.02 遍历 $0.733$~$0.933$，再在 $0.78$~$0.86$ 内以 0.01 为步长细化，净成本在 $\tau=0.81$ 处最低，为 1495.38 万元，按费用最低的原则取 $\tau=0.81$ 作为决策分位，0.833 只作为单时段代价比给出的参考值。



\subsubsection{计划购电模型}
\label{sub:p2build}

计划层沿用问题~1 的供需平衡、光伏分流和储能动态关系，但取消每天 24:00 的储电量必须回到 00:00 的约束。设 $E_0^{(d)}$ 为第 $d$ 天初始储电量，首日取 6000~kWh，之后取前一日实际执行期末的储电量。日末储能仍可供后续时段使用，按日末时刻电价与 90\% 的可利用率估计其价值 $0.9\lambda_{144}E_{144}^{(d)}$，并从当日计划购电成本中扣除，使 LP 不会为了压低当日成本而在午夜前放空储能。这项扣除是优化模型对剩余电量的估值，并非向外网售电，题面也没有给出售电价格，储能中的电量只能在之后的时段放电使用。完整模型为
\begin{equation}
\begin{aligned}
\min\quad & F^{(d)}=\sum_{t=1}^{144}\lambda_t\,(a_t+b_t)\,\Delta t-0.9\,\lambda_{144}\,E_{144}^{(d)} \\
\text{s.t.}\quad
& a_t+g_t+d_t=\hat L_t,\qquad g_t+c_t+q_t=\hat P_t, \\
& E_t=E_{t-1}+0.9\,(b_t+c_t)\,\Delta t-\frac{d_t}{0.9}\,\Delta t,\\
& 1200\le E_t\le 10800,\qquad b_t+c_t\le 5000,\qquad d_t\le 5000,\\
& a_t,\,b_t,\,g_t,\,c_t,\,d_t,\,q_t\ge 0,\\
& t=1,\dots,144 .
\end{aligned}
\label{eq:p2full}
\end{equation}

SOC 消元后，每日 LP 含 864 个决策变量、288 个等式约束与 576 个不等式约束，用 HiGHS 逐日滚动求解 334 天。储能价值只对日末存量计一次，不把每个时段的 $E_t$ 逐项计入目标，否则同一批电量会因在储能中停留而被重复扣减。计划购电量按 $(a_t+b_t)\Delta t$ 计费，实际执行后的期末 SOC 直接作为次日初值；放电按计划执行，仅在触及 1200~kWh 底线时截断，全天储电量保持在 $[1200,10800]$~kWh 内；充电由计划购电充电量与实现的光伏盈余构成，受功率与容量上限截断。求解中对同段充放的退化解加极小吞吐项，该项不计入报告成本。

\subsubsection{紧急购电与结算}
\label{sub:p2settle}

计划在每日 0:00 锁定，日内不再根据实际负荷和光伏调整。当计划供给不足时，紧急购电承担供电平衡的硬约束。记实际光伏功率为 $P_t$，计划光伏直供为 $g_t$，实际供给量为
\begin{equation}
S_t=a_t+g_t^{r}+d_t ,\qquad g_t^{r}=\min(g_t,\,P_t) ,
\label{eq:supply}
\end{equation}
其中 $P_t$ 为该时段光伏实际功率，$g_t^{r}$ 表示实际光伏先满足计划直供，不足部分即是缺口的来源之一。缺口与紧急购电费为
\begin{equation}
e_t=\max\bigl(0,\;L_t-S_t\bigr),\qquad \text{紧急费用}=5\lambda_t\,e_t\,\Delta t ,
\label{eq:gap}
\end{equation}
连续缺口时段合并为同一区间记账。此外，实际光伏超出计划直供的部分并入充电，受功率与容量上限截断，装不下的部分弃掉。全年能源支出为计划购电费与紧急购电费之和，净成本再减去期末储能价值。

\subsection{模型对比}
\label{sub:p2result}

三组预测来源使用同一个计划 LP 和同一套实际数据结算规则，表~\ref{tab:p2comp} 比较其全年净成本与紧急购电缺口。模型池组、XGBoost 对照组与 LSTM 组的净成本分别为 1638.75、1710.30 与 2829.85 万元，完美预见下界为 1225.12 万元，模型池组比下界多支出 413.63 万元。三组的差别只有预测曲线来源，因此表中的费用差异全部来自预测质量。

\begin{table}[htbp]
\centering
\caption{三组预测来源的全年模型对比（2025.2.1--12.31，费用单位：万元）}
{\small
\begin{tabular}{lccc}
\toprule
方案 & 紧急费 & 净成本 & 缺口/kWh \\
\midrule
模型池组 & 413.39 & 1638.75 & 953,922 \\
XGBoost 对照组 & 492.92 & 1710.30 & 1,120,334 \\
LSTM 组 & 1633.96 & 2829.85 & 3,661,104 \\
\bottomrule
\end{tabular}}
\label{tab:p2comp}
\end{table}

模型池组的净成本最低，为 1638.75 万元，比 XGBoost 对照组低 71.55 万元，比 LSTM 组低 1191.10 万元。XGBoost 对日历、滞后量等表格特征的非线性关系处理直接，净成本仅为模型池组的 1.044 倍，说明通用的梯度提升模型仍能保持接近集成模型的结果。LSTM 具备提取长短期序列依赖的能力，但本题纯时序基线的负载 MAE 为 682.08~kW，模型池组为 162.99~kW；缺少日历特征且训练窗口固定，使其在 6~7 月与 10~11 月的季节切换期误差进一步放大，计划层系统性少购电，紧急购电费升至 1633.96 万元。

从费用构成看，模型池组的计划购电费为 1225.41 万元、紧急购电费为 413.39 万元；XGBoost 对照组为 1217.43 万元与 492.92 万元；LSTM 组为 1195.94 万元与 1633.96 万元。LSTM 的计划费是三组中最低，却因缺口多付了数倍于计划费节省的紧急费用，净成本反而最高。计划买得少不等于总成本低，预测模型的比较必须放到结算后的费用上。


图~\ref{fig:p2monthly} 给出三组预测来源的月度紧急购电费用。三组的紧急费都在夏季抬升，6 月同时达到峰值，对应全年预测误差最强的时段；LSTM 组在各月的紧急费都明显高于另外两组，说明其缺口并不集中在某几个月，而是全年持续存在。

\begin{figure}[htbp]
\centering
\includegraphics[width=0.95\textwidth]{fig6_p2_monthly.png}
\caption{三组预测来源的月度紧急购电费用对比}
\label{fig:p2monthly}
\end{figure}

% 问题 2 的模型评价暂缓，待全文最后统一撰写。
% 原拟要点：计划层与问题 1 同构、紧急购电按题面硬约束结算、三组预测来源在同一结算层比较；
% 缺点为残余缺口、分位覆盖率 75.0% 偏低、固定分位档跨季节不自适应。

\section{问题3模型的建立与求解}
% TODO：附件3 提供每日 0/6/12/18 点的未来 24 小时光伏预报；0:00 制定计划购电，
% 其他时刻制定调整购电；计划高于调整为违约部分按 0.5 倍价、调整为超购部分按 1.5 倍价；
% 需分析是否值得引入其他时刻的预报；输出 result3.xlsx。

\section{问题4模型的建立与求解}
% TODO：附件4 波动电价替代分时电价，重算问题2与问题3；输出 result4-2.xlsx、result4-3.xlsx。

% ===================== 9 总结（占位） =====================
\section{模型评价与总结}
% TODO：逐条回答四个问题的最终数值结论；优缺点对着模型说并量化（效率口径 ±、
% 完全信息假设的局限）；不复述摘要，不引入新内容。

% ===================== 参考文献 =====================
{\small
\begin{thebibliography}{10}
\bibitem{wangxz2017} 王先齐, 吕智林, 汤泽琦. 基于分时电价机制的并网型微网多目标动态优化调度[J]. 电力系统保护与控制, 2017, 45(4). DOI: 10.7667/PSPC160250.
\bibitem{yuanlong2018} 袁龙. 分布式光伏发电与储能协同调度模型研究[D]. 济南: 山东大学, 2018.
\bibitem{sioshansi2009} Sioshansi R, Denholm P, Jenkin T, et al. Estimating the value of electricity storage in PJM: Arbitrage and some welfare effects[J]. Energy Economics, 2009, 31(2): 269-277.
\bibitem{wangly2020} 王凌云, 王晓敏, 胡兴媛, 等. 基于多智能体微网的需求侧分时电价优化调度[J]. 信息与控制, 2020, 49(4): 429-435.
\bibitem{boyd2004} Boyd S, Vandenberghe L. Convex Optimization[M]. Cambridge: Cambridge University Press, 2004.
\bibitem{che2022} 车泉辉, 陈建洪. 光伏功率短期预测方法研究综述[J]. 电力自动化设备, 2022, 42(10): 12-21.
\bibitem{arrow1951} Arrow K J, Harris T E, Marschak J. Optimal inventory policy[J]. Econometrica, 1951, 19(3): 250-272.
\bibitem{cleveland1990} Cleveland R B, Cleveland W S, McRae J E, et al. STL: A seasonal-trend decomposition procedure based on Loess[J]. Journal of Official Statistics, 1990, 6(1): 3-73.
\bibitem{lu2019} Lu J, Liu A, Dong F, et al. Learning under concept drift: A review[J]. IEEE Transactions on Knowledge and Data Engineering, 2019, 31(12): 2346-2363.
\bibitem{wagnermuns2018} Wagner-Muns I M, Guardiola I G, Samaranayke V A, et al. A functional data analysis approach to traffic volume forecasting[J]. IEEE Transactions on Intelligent Transportation Systems, 2018, 19(3): 878-888.
\bibitem{akyurek2014} Akyurek B O, Akyurek A S, Kleissl J, et al. TESLA: Taylor expanded solar analog forecasting[C]. 2014.
\bibitem{mourtada2019} Mourtada J, Ga\"{i}ffas S. On the optimality of the Hedge algorithm in the stochastic regime[J]. Journal of Machine Learning Research, 2019, 20(83): 1-46.
\bibitem{graefe2015} Graefe A, K\"{u}chenhoff H, Stierle V, et al. Combining forecasts: Evidence on the relative accuracy of the simple average and Bayesian model averaging[J]. Statistical Modelling, 2015.
\bibitem{wang2019cqra} Wang Y, Zhang N, Tan Y, et al. Combining probabilistic load forecasts[J]. IEEE Transactions on Smart Grid, 2019, 10(4): 4477-4489.
\bibitem{zhang2023onenet} Zhang Y F, Wen Q, Wang X, et al. OneNet: Enhancing time series forecasting models under concept drift by online ensembling[C]//Advances in Neural Information Processing Systems (NeurIPS). 2023.
\bibitem{sun2021tvjma} Sun Y, Hong Y, Lee T H, et al. Time-varying model averaging[J]. Journal of Econometrics, 2021, 222(2): 974-992.
\end{thebibliography}
}

% ===================== AI 使用声明（占位） =====================
\section*{AI 工具使用声明}
% TODO（2026 新规）：按竞赛当期规范声明 AI 工具的使用环节与方式，并在支撑材料中附
% 《AI工具使用详情.pdf》（工具版本/目的环节/提示方式/采纳核验情况）。
（占位）本项按竞赛当期规范填写。

\end{document}
