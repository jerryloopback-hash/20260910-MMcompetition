# 论文合并验收报告 —— 问题2预测模块 → Cpaper.tex

> **验收人**：dsh
> **依据**：`交接文档_论文合并_dsh.md` 第五节验收清单
> **日期**：2026-09-12
> **结论**：✅ **验收通过。7 处补丁全部就位、无丢失、无回退；编译三遍零 undefined；PDF 抽查正常。本次未对 `Cpaper.tex` 做任何改动。**

---

## 一、验收清单逐项结果（交接文档 §5）

| # | 判据 | 实测结果 | 结论 |
|---|---|---|---|
| 1a | `grep 'fig_p2f'` ≥ 6 | 字面命中 3（= 3 个 `\includegraphics` 文件名） | ⚠️ 判据写法需修正，见下方说明 |
| 1b | `grep 'ln N/T'` 命中 | 命中 2 处（486 行 μ 推导句、629 行设计层复述） | ✅ |
| 1c | `grep '等权'` 问题2预测部分不命中 | 全文 0 命中（另查 `均等`/`平均权重`/`154.44` 亦为 0） | ✅ 方案 b 合规 |
| 2 | 编译两遍 + log 无 undefined/missing | 编译三遍，exit 0，22 页，log 中 undefined / Missing / No file / Citation undefined **均为 0** | ✅ |
| 3 | PDF 抽查：冷启动表、三层三段、两张新图 | 全部正常显示，见第四节 | ✅ |

**关于 1a 判据的说明**：三张新图确实全部就位，但 `\label` 采用的是冒号风格（`fig:p2fperiod` / `fig:p2foracle` / `fig:p2fsens`），
只有 `\includegraphics` 的文件名才含下划线，故字面 `grep 'fig_p2f'` 只计到 3。
按"文件名 + label + ref"合并统计（正则 `fig.p2f`，排除 `fig:p2flow`）共 **9 处引用**，远超阈值 6：

- `\includegraphics` 3 处：`fig_p2f_period.png`(618)、`fig_p2f_oracle.png`(633)、`fig_p2f_sens.png`(640)
- `\label` 3 处：620、635、642
- `\ref` 3 处：614、627、629

---

## 二、补丁就位核验（与合并前备份逐行 diff）

对照件：`D:\CUMCM2026Problems\C题\Cpaper_backup_合并前_20260912.tex`（58066 B）
现行件：`outcome\Cpaper.tex`（62933 B）。净变化 26 行删、60 行增。

| 补丁 | 现行行号 | 核验内容 | 结论 |
|---|---|---|---|
| B3 | 477–479 | 权重动机重写为纯结构性理由（阶段主导因素不同 / 全期误差只是各阶段平均 / 逐阶段人工定权负担过重），**无任何精度对比** | ✅ |
| F3 | 486 | μ 由 `μ*=ln N/T`（N=5、T=334、≈0.0048）**推导**得出，非调参 | ✅ |
| C3 | 524、526–542 | 原 TODO 与行内半成品文字已替换为 `tab:coldstart` 冷启动权重表（表 4） | ✅ |
| D3 | 591 | "负载池初始 7 模型剔除 FPCA 与谐波回归的精简决策，同样出自这组数据。"一句已删除 | ✅ |
| E3 | 612 | 段末句已改为"…这正是选择在线权重而非**人工定权**的理由" | ✅ |
| H3 | 614 + 616–621 | 分时段对照段 + 图（图 5，`fig:p2fperiod`） | ✅ |
| G3 | 623–643 | 过拟合检验三层重写：参数层 / 权重层 / 设计层，配图 6（`fig:p2foracle`）、图 7（`fig:p2fsens`） | ✅ |

**附带的非补丁改动（均为增量、无回退）**：模型池成员清单由 `a./b./c./d./e.` 改为 `（1）～（5）` 体例，
并新增"选型三原则"与"实证精简（FPCA/HarmonicReg 边际贡献为负）"两段。属交接文档 §7 划定的**队友自有段落**，未干预。

**行号漂移说明**：交接文档 §4 标注的"原 477–484 / 原 491 / 原 525"是**被替换的原文行号**；
补丁本身使 B3 段由 8 行缩为 3 行，其后所有内容前移 5 行，与实测完全吻合，**并非队友事后改动**。

---

## 三、编译

```
xelatex -interaction=nonstopmode -halt-on-error -output-directory=out Cpaper.tex   # 三遍
```

- 三遍均 exit 0，`Output written on out\Cpaper.pdf (22 pages)`
- `out\Cpaper.pdf` 已更新（1903722 B，20:51:05）
- log 诊断：undefined references = 0；Missing character = 0；No file / not found = 0；Citation undefined = 0
- 仅有的提示是 MiKTeX 未检查更新（`major issue: So far, you have not checked for MiKTeX updates.`），无害

---

## 四、PDF 抽查（渲染页逐页目视）

| 页 | 内容 | 结果 |
|---|---|---|
| 14 | 表 4 冷启动阶段两模型池的初始权重（0.25/0.30 … 0.15/0.20，五成员对齐） | ✅ 排版正常 |
| 15 | 表 5 池内单模型与 EGD 集成 MAE；表 6 消融；E3 段末句；H3 分时段段 | ✅ |
| 16 | 图 5 分时段对照（XGBoost/LSTM 三色柱，2–4 月与 5–12 月两窗）；参数层/权重层/设计层三段 | ✅ 图清晰、数字与正文一致 |
| 17 | 图 6 泄漏核验（负载 172.71/162.99/139.59；光伏 160.71/165.96/140.17）；图 7 设计层敏感性（全部落在 ±2% 灰带内，最大 +1.95%） | ✅ |

抽查未发现图片缺失、错位、乱码或占位框。

---

## 五、遗留事项（均不阻塞提交，与交接文档 §8 一致）

1. **队友笔误仍在**（属队友段落，未代改）：520 行"随着数据量的增加**啊**"；521 行 `\cite{zhang2023onenet}` 后缺句号。
2. **仍为 TODO 的章节**（队友职责）：摘要、问题2模型评价、问题3/4、总结、AI 使用声明。
3. **LSTM 双版本**：正文与图/DM 检验用生产版 682.1 / 443.4（584 行）；
   费用回测口径用仓库复跑版。已核查 `Cpaper.tex` 中费用回测段（721 行）只报净成本、不报 LSTM MAE，
   **论文内部未见数字冲突**，暂无需处理。
4. 本节报告与 `_overfit_*` 审计件同属内部件，**不入国赛提交包**。

---

## 六、复算 / 回滚入口

- 重新编译：`cd outcome` 后执行第三节命令（两遍即可解析交叉引用）
- 回滚原件：`D:\CUMCM2026Problems\C题\Cpaper_backup_合并前_20260912.tex`
- 补丁锚点脚本：`D:\CUMCM2026Problems\C题\_apply_patches_preview.py`（每补丁断言"恰好命中一处"）
