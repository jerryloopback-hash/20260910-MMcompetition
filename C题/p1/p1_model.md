# 问题1（P1）数学模型：计划购电策略

> **版本状态**：v2 日历对齐修订版，**已重跑刷新**（2026-09-11）。`result1.xlsx`、`_traj.npy` 为 v2 产物；`figs/` 为论文图；`_sensitivity.npz`、`_greedy.npz` 为 §10 数据。
> **v2 结果摘要**：全天购电量 59,482.7 kWh，购电费 **35,126.85 元**（无储能基线 48,052.05 元，**省 26.9%**）；弃光 0；E(0:00)=E(24:00)=6000；六项自检全过（守恒差 1.5e-11、KKT 违约 ≤1.4e-17、有限差分验证偏差 1.2e-12）。
> **复现**：`python3 -m venv venv && venv/bin/pip install numpy scipy openpyxl matplotlib`，依次运行 `p1_solve.py` → `p1_sensitivity.py` → `p1_greedy.py` → `p1_figs.py`（路径均相对脚本定位）。

## 1. 问题重述（P1 相关部分）

- 附件1 给出某一天（"某天"）每 10 分钟的：电价、小区负载、光伏发电预测功率。
- 每天电价与负载相同（即附件1 的这天是周期性重复的一天），光伏用预测值。
- 需求：微网供电逐时段不低于负载；储能 0:00 与 24:00 电量相同（附录1 初值 6000 kWh）。
- 目标：满足负载约束下最小化全天购电费。
- 输出：按 附件5/result1.xlsx 模板，填 144 个 10 分钟购电量 + 6 个 4 小时块的充放电量 + 0:00/24:00 储电量。

## 2. 数据读取与时段对齐（v2 修订核心）

### 2.1 附件1 的真实结构

附件1 共 144 行数据，粒度 10 分钟。时间列**前 60 行是数值（天内小数，0:10→10:00），后 84 行是共享字符串文本（"10:10"→"24:00"）**——直接按数值解析后 84 行会得到错误的"字符串索引"数据，解析时必须区分类型（pandas/openpyxl 会自动处理；本目录曾因此误判数据"跳变"）。行标签语义为**时段起点**：第 1 行标 0:10 = 时段 [0:10,0:20)，……，第 143 行标 23:50 = [23:50,24:00)，第 144 行标 0:00+1（=次日 0:00）= [24:00,24:10)。

### 2.2 日历时段定义与数据映射

定义日历时段 **I_k = [10(k−1), 10k) 分钟，k=1..144**，即 [0:00,0:10) … [23:50,24:00)。数据映射：

| 日历时段 | 数据来源 | 依据 |
|---|---|---|
| I_1 = [0:00,0:10) | 附件1 第 144 行（标签 0:00+1） | 该行是次日首 10 分钟；由"每天电价/负载相同 + 光伏预测为同一日曲线"的**逐日周期性**，其数值与当日 [0:00,0:10) 完全相同 |
| I_k（k=2..144） | 附件1 第 k−1 行 | 标签=时段起点，直接对应 |

映射后 144 个日历时段恰覆盖当日 0:00–24:00，无缺失、无重叠（已用脚本对附件1 全部行标签做自测验证）。

### 2.3 为什么放弃 v1 的对齐（问题记录，供论文"模型修订"叙述）

v1 直接把附件1 的 144 行当作优化时段（即 [0:10,0:20)…[24:00,24:10)），并在时段 143 末（=24:00）钉 E=6000、时段 144 末端 E 自由。后果：

1. **末端效应**：时段 144 [24:00,24:10) 在目标函数里"后面没有明天"，LP 把储能偷放到 5362 kWh（该段购电 0、放电 574 kWh）——方案**不可逐日复制**（明天 0:10 的初始电量不再是 6000），与"每天制定同一策略"的题设矛盾；目标函数同时少计约 245 元。
2. **表2 自洽性破裂**：v1 表2 六块回推 ΔSOC = 0.9×Σ充 − Σ放/0.9 = −637.9 kWh ≠ 0，与表内"0:00/24:00 储电量均为 6000"直接矛盾，评委复算即可发现。
3. **窗口错位**：v1 的 4 小时块实际覆盖 [0:10,4:10) 等 +10 分钟偏移窗口，且第 6 块混入了跨日段。

v2 用日历对齐 + **E(0:00)=E(24:00)=6000 双端钉死**，三类问题同时消失：末端效应不存在（[0:00,0:10) 是正常的日谷充电段，其充电价值通过次日早峰完整进入目标函数）；表2 块与日历窗口精确对齐；模板末行获得自然解释（见 §8）。

### 2.5 修订的实测效果（重跑核对，修正此前估计）

重跑结果：v2 精确循环最优 **35,126.85 元**。对照 v1：其报告值 34,881.45 元中含跨日段"偷放"少计的 245.49 元（574.1 kWh × 0.4276 元/kWh），诚实口径约 35,126.94 元——**v2 与 v1 诚实口径基本持平（−0.09 元）**。原因：夜谷足够长，v1 实际只是把谷充窗口平移了 10 分钟，[0:00,0:10) 的充电价值已被其余谷段等价实现。因此本次修订的价值在于**方案可逐日复制、表2 自洽、模板行语义正确**（可复算性），而非降本；此前"1–2% 成本改善"的估计不成立，已在本文档更正。

### 2.4 模板行映射

模板"计划购电量"共 144 数据行，标签 0:10-0:20 … 23:50-0:00+1、0:00+1-0:10+1：

- 行 r（r=2..144）↔ 日历时段 I_r（标签起点 10(r−1) 分钟，与 I_r 定义一致）；
- 行 145（0:00+1-0:10+1 = 次日 [0:00,0:10)）↔ **I_1 的周期镜像**：由于逐日执行同一策略，次日首段的购电量等于当日 I_1 的购电量，直接复制填写。

因此报告的 144 个购电量按周期平移恰好覆盖全部时间——模板、附件1、优化模型三者完全闭合。

## 3. 参数（由附件1 读入，按 §2.2 映射到 I_1..I_144）

| 记号 | 含义 | 单位 |
|---|---|---|
| Δt | 时段长（=1/6 h） | h |
| λ_t | 外网电价 | 元/kWh |
| L_t | 小区负载 | kW |
| P_t^{PV} | 光伏发电预测功率 | kW |

附录1 储能参数：容量上限 12000 kWh，最大充/放功率 5000 kW，运行电量带 [1200, 10800] kWh，充、放电效率各 90%，0:00 初值 6000 kWh。

## 4. 决策变量（每时段 6 个 + 隐式 SOC）

| 记号 | 含义 | 单位 |
|---|---|---|
| a_t | 外网购电直供负载 | kW |
| b_t | 外网购电充电 | kW |
| g_t | 光伏直供负载 | kW |
| c_t | 光伏充电 | kW |
| d_t | 储能放电供负载 | kW |
| q_t | 弃光功率（松弛量） | kW |

储电量 E_t 不作为独立变量，用"净充电累计"P_t = Σ_{s≤t}[0.9(b_s+c_s) − d_s/0.9]Δt 隐式表达，E_t = 6000 + P_t（降低规模并使 SOC 约束成为稀疏累计不等式）。

## 5. 目标函数

$$\min \; F=\sum_{t\in I_1..I_{144}} \lambda_t\,(a_t+b_t)\,\Delta t$$

全天购电量 = Σ(a_t+b_t)Δt，购电费 = F。

## 6. 约束

1. 负载平衡：a_t + g_t + d_t = L_t（等式不失一般性：多供无益，LP 自动取等）
2. 光伏平衡：g_t + c_t + q_t = P_t^{PV}
3. 储能动态：E_t = E_{t−1} + 0.9(b_t+c_t)Δt − (d_t/0.9)Δt（充、放各 90%，往返 0.81）
4. 容量界限：1200 ≤ E_t ≤ 10800（所有节点）
5. 功率界限（**并网侧口径**，见注记 5）：b_t + c_t ≤ 5000，d_t ≤ 5000
6. 边界条件：E_0 = 6000（附录1 初值）；E_144 = 6000（0:00 与 24:00 储电量相同 + 逐日重复 + 初值 6000 ⇒ 终值必为 6000）
7. 非负：a_t, b_t, g_t, c_t, d_t, q_t ≥ 0（不售电：购电量与放电量均不可为负；放电被 a,g ≥ 0 自动限制在 d_t ≤ L_t 内，不会反送外网）

## 7. 求解方法

- 模型为标准 **LP**：变量 6×144 = 864（E 以累计和隐式表达），等式约束 2×144+1，不等式约束 4×144。
- 求解器：**HiGHS**（经 `scipy.optimize.linprog(method='highs')` 调用），毫秒级。
- 配套脚本：`p1_solve.py`（主求解+全部校验+写结果）、`p1_sensitivity.py`（η/PMAX 扫描，§10）、`p1_greedy.py`（规则式对照，§10）、`p1_figs.py`（论文图，§9.5）。
- 结果校验清单（脚本自动执行）：
  1. E(24:00) = 6000（断言）；
  2. 全天能量守恒恒等式：购电+光伏 = 负载+弃光+充损+放损；
  3. 无同段边充边放（计数 = 0）；
  4. 表2 自洽：0.9×Σ充电量 − Σ放电量/0.9 ≈ 0 = E(24:00)−E(0:00)；
  5. 对偶/影子价格提取 + KKT 互补松弛校验 + 有限差分交叉验证（§9）；
  6. 写入 xlsx 后按**发布值（舍入后）**复算全天购电量。
- 弃光 q_t 作松弛保留。本例最优解 q ≡ 0：全天光伏盈余远小于可充容量与负载转移能力之和，"该日裕量充足、光伏全额消纳"是可写入结论的观测。

## 7.5 建模注记（写入论文假设与说明一节）

1. **效率口径：充、放各 90%**（往返 0.81）。若理解为往返合计 90%，成本差约 8%。90% 只作用于进出储能的路径，购电/光伏直供负载不打折（题目只给了储能效率）。
2. **时段对齐与周期性**（v2 修订，见 §2）：I_1 用附件1 第 144 行数据的合法性来自逐日周期性——每天电价、负载相同，光伏预测为同一日曲线，故"次日 0:00-0:10"与"当日 0:00-0:10"数据相同。该假设应在论文中显式声明。v1 的跨日段末端效应（不可逐日复制、表2 差 −638 kWh）由此修订消除；预期改善为目标函数口径 ≥245 元/日，加上 [0:00,0:10) 恢复正常谷段充电后的重优化空间，合计约 1–2%（以重跑为准）。
3. **弃光**：作为松弛变量保留；本例最优恒为 0（见 §7 校验清单）。
4. **无需 0-1 变量排除"边充边放"**：由 §9 对偶分析，同时充放要求节点边际价值 γ_t 与电价同时为零，正电价下 LP 自动排除（脚本对最优解逐段复核为 0 段）。
5. **功率上限 5000 kW 取并网侧（母线端）口径**：充电时从母线取电 ≤5000 kW（计入变流器损耗后电池实际存入 4500 kW）；放电时馈入母线 ≤5000 kW（电池实际放出 5556 kW）。理由：①储能系统额定功率的行业惯例是交流端铭牌值；②与"效率损耗发生在变流器"的物理图像自洽；③充/放两方向在母线电表上对称，与题面单一数值表述匹配。敏感性（§10 PMAX 扫描）：放电峰值 4294 kW，两种口径均不触顶；充电有 19 个时段顶格，但 **PMAX 5000→6000 成本仅降 0.12%**，故若改用电池侧口径（等效充电上限 5556 kW）成本降幅 <0.12%——口径影响存在但很小，仍须在论文假设中写明。
6. **表2 记账口径（口径乙）**：充电量 = Σ(b_t+c_t)Δt（并网侧输入电能量），放电量 = Σd_tΔt（并网侧送达电能量）；于是 0.9×充电量 − 放电量/0.9 = 储电量变化，表2 与储电量列自洽。放电量≠负载（负载由购电直供+光伏+放电三源共同满足）。
7. **多重最优**：144 段电价有 143 个互异值（仅 1 处两段同价），LP 最优解在报告量（购电量、费用、充放电量）上基本唯一；a/b（购电去向）与 g/c（光伏去向）的拆分退化不影响任何报告量。

## 8. 输出约定（result1.xlsx）

- "计划购电量"：行 r（2..144）填 I_r 购电量 (a_r+b_r)Δt（kWh）；行 145 填 I_1 的周期镜像值（§2.4）。
- "充放电量"：6 个块与日历窗口对齐（0-4,4-8,…,20-24 点 = I_{24k+1}..I_{24k+24}）；充电量 = Σ(b+c)Δt，放电量 = Σd·Δt（口径乙，注记 6）；0:00 与 24:00 储电量均填 6000。
- 表1（论文）：全天购电量 = Σ_{I_1..I_144}(a+b)Δt，购电费 = F；六个指定时段 10:00-10:10 … 20:00-20:10 对应 I_61、I_73、I_85、I_97、I_109、I_121（模板行 61/73/85/97/109/121）。
- `_traj.npy` 存档行序：a, b, g, c, d, q, a+b, SOC[1..144], γ（节点边际价值，若 scipy 返回对偶）。

## 9. 对偶分析：影子价格与充放电阈值（论文素材）

### 9.1 出发点：LP 的解为什么长这样

LP 求出的计划呈现清晰的块结构：夜谷充电（0:10-1:10 顶格）、午间光伏充电、早晚双峰放电、其余时段按需直购、中午光伏自给后购电为 0。LP 并不懂"低买高卖"——这些结构由**对偶变量（影子价格）**严格决定。本节把直觉变成可检验的互补松弛条件，既是最优性的独立验证，也是论文中解释机制的核心素材。

### 9.2 对偶变量一览与节点边际价值

给各类约束挂上拉格朗日乘子：负载平衡 α_t（**边际供电成本**，即瞬时现货价 σ_t = α_t/Δt）、光伏平衡 β_t（光伏的边际机会价值）、SOC 上下界 η_t, θ_t ≥ 0、充/放功率上限 π_t, ρ_t ≥ 0、全天归零约束的影子价 ϖ。

核心量是**节点边际价值 γ_t**——在节点 t（时段 t 末）白得 1 kWh 储能可节省的费用：

$$\gamma_t = \gamma_{t+1} - \eta_t + \theta_t,\qquad \gamma_{145}:=\varpi$$

递推读法（与 §9.4 数值曲线互相印证）：

- **内点节点**：γ 逐段不变——能量躺在罐里不生息，价值跨时间守恒；
- **满罐节点**（η_t > 0）：γ 向下台阶——罐满时多出的 1 kWh 挤占罐容，价值被"上限稀缺"的影子价削低；
- **空罐节点**（θ_t > 0）：γ 向上台阶——能量枯竭边缘，1 kWh 格外珍贵。

γ 的水平由充电/放电条件在窗口两端"锚定"（下述 9.3），因此整条 γ 曲线是分平台、在充放窗口间爬坡的阶梯状曲线——它就是**储能在各时刻的影子价格**，是论文中最能体现"模型在思考"的一张图（γ_t 与 λ_t 同轴对比）。

### 9.3 三条经济判据（KKT 互补松弛）

以 σ_t 记节点 t 的边际供电成本（a_t>0 时 σ_t = λ_t；放电自给时 σ_t = γ_t/0.9 + ρ_t/Δt）：

| 决策 | 互补松弛条件 | 经济读法 |
|---|---|---|
| 充电 b_t+c_t > 0 | λ_t = 0.9γ_t − π_t/Δt | 买入 1 kWh → 存入 0.9 kWh，存入价值恰好等于电价；功率顶格时出现正溢价 π/Δt |
| 放电 d_t > 0 | σ_t = γ_t/0.9 + ρ_t/Δt | 放出 1 kWh 送达 0.9 kWh，其价值恰好等于边际供电成本 |
| 空闲 | 0.9γ_t − π_t/Δt ≤ λ_t ≤ γ_t/0.9 + ρ_t/Δt | **迟滞带**：现价落在 [0.9γ, γ/0.9] 内时不动作——带宽因子 1/0.81 就是往返损耗造成的"无套利区间" |

推论一（**套利阈值**）：从时段 s 充、时段 t 放，中间无边界节点时 γ_s = γ_t，联立两等式得 λ_t/λ_s ≥ 1/0.81 ≈ **1.2346**：价差比不足 1.2346 的两段之间绝不搬运能量。这是全部启停决策的统一判据。

推论二（**无同充同放**）：同段既充且放要求 λ_t = 0.9γ_t 且 λ_t = γ_t/0.9 同时成立 ⇒ γ_t = 0 且 λ_t = 0。本题电价恒正，故最优解必无同段充放——这就是注记 4 的理论依据，无需引入 0-1 变量。

推论三（**弃光条件**）：q_t > 0 ⟺ β_t = 0 ⟺ 光伏边际价值为零（罐满且负载已满覆盖）。本例 q ≡ 0 说明每一 kWh 光伏都被消纳。

### 9.4 数值验证（v2 实跑结果，脚本自动生成）

脚本对最优解逐段校验 9.3 的互补松弛（充电段等式、放电段等式、空闲段迟滞带），并输出：

1. **互补松弛违约 ≤ 1.4×10⁻¹⁷ 元/kWh**（充电/放电两侧均为机器精度级）——三条判据在最优解上严格成立；
2. **有限差分交叉验证**：抽样 8 个内点节点，在节点挂零成本、上限 5 kWh 的"自由能量"变量重解 LP，目标函数斜率与 −γ_t 的最大偏差 **1.2×10⁻¹²**——对偶符号约定经两种独立方法认证。（注意：不能用"钉住 P_i=δ"做此实验——那是强迫多充、含获取成本，不是白得能量；初版 FD 检验即因此报出 332 元/kWh 假偏差，已修正。）
3. **γ 全天曲线**：范围 [0.471, 1.130] 元/kWh，平台-台阶结构清晰（图 figs/fig3_shadow.png）；
4. **充/放窗口表**（σ 为边际供电成本）：

| 窗口 | 电量 (kWh) | λ 范围 | γ | 机制读法 |
|---|---|---|---|---|
| 充 0:00-1:00 | 4500 | 0.424-0.428 | 0.475 | 谷充：λ≈0.9γ（顶格段 π>0） |
| 充 5:40-5:50 | 833 | 0.371 | 0.475 | 全日最低价段补满至 10800 |
| 放 6:00-9:40 | 8068.8 | 0.690-1.168 | 0.532 | 早峰：σ=γ/0.9=0.591<λ，**放电全覆盖负载、购电为 0** |
| 充 9:40-14:40 | 9961.5 | 0.411-1.027 | 0.532 | 午间：光伏盈余充电为主 + 低价段补购 |
| 放 14:40-15:00 | 91.1 | 0.938-1.091 | 0.733 | 午后小峰微放 |
| 放 18:10-18:20 | 109.3 | 1.256 | 1.130 | 晚峰前沿 |
| 放 18:50-20:50 | 8530.7 | 1.288-1.395 | 1.130 | 晚峰：σ=1.256<λ，储能全覆盖 |
| 充 22:00-23:40（分散） | ≈5333 | 0.386-0.424 | 0.471 | 夜谷回充，闭环至 6000 |

值得写进论文的两个结构观测：①两个放电大窗口内 σ=γ/0.9 **低于**当时电价（0.591 < 0.69-1.17；1.256 < 1.29-1.40）——储能把两个价格峰完全"削平"，峰段购电为零（表1 的 10:00-10:10、14:00-14:10、20:00-20:10 购电量均为 0）；②满罐平台段（15:10-18:30，SOC=10800）γ 从 0.733 爬升至 1.130——每经过一个满罐节点 γ 上移 η_t，罐容稀缺的影子价随放电临近累积，这是内点"价值守恒"规则在边界约束下的变形。

### 9.5 论文图设计（精而不多：3 图 + 3 表）与引用支撑

**图**（`figs/`，300 dpi，可直接插入论文）：

| 图 | 文件 | 内容 | 承载论点 |
|---|---|---|---|
| 图1 | fig1_data.png | 电价/负载/光伏数据总览（双 y 轴） | 双谷双峰价格结构 → 套利空间存在性 |
| 图2 | fig2_dispatch.png | SOC 轨迹（上）+ 购电/充/放功率（下） | 计划全貌：日双循环、两端 6000、顶格充电 |
| 图3 | fig3_shadow.png | γ 与 λ 同轴 + 充/放窗口着色 | §9 机制：阈值切换、迟滞带、影子价格 |

**表**：表1（六时段购电量 + 全天量费）、表2（四小时块充放电量 + 储电量）、表3（§10.1 敏感性小表）。不再增图——数据总览、计划结果、机制解释三层已闭环，更多图会稀释重点。

**引用支撑**（正文挂靠点 → 文献编号，见文末参考文献表）：

- "充、放效率各取一值（各 90%）" → 文献[7]（王先齐篇案例设充 93%/放 92% 两个独立效率）；文献[1][2] 用单侧 η 记法——注记中写明"两种记法文献中均有采用"。
- "价差与往返效率决定套利盈亏平衡（阈值 1/0.81）" → 文献[1][2]。
- "完全信息下价格接受者储能调度用 LP 是标准做法" → 文献[1][3][4]。
- "规则式谷充峰放是文献常见路线（本文以 LP 精确化并量化差距，§10.2）" → 文献[5][7]。
- "对偶/影子价格与边际电价的理论基础" → 文献[8]（LP 对偶与 KKT：Boyd & Vandenberghe）、文献[9]（边际电价思想：Schweppe et al.）。

## 10. 敏感性与规则式策略对照（论文各用一小段即可）

### 10.1 参数敏感性（完整 LP 重解，`p1_sensitivity.py`）

题面给定的 90% 效率是硬数据，本扫描只为展示**模型对参数的响应结构**，不用于改参数：

| η（充、放各 η） | 套利阈值 1/η² | 购电费（元） | 较 η=0.90 |
|---|---|---|---|
| 0.85 | 1.3841 | 36,603.21 | +4.20% |
| 0.88 | 1.2913 | 35,696.82 | +1.62% |
| **0.90（题面）** | **1.2346** | **35,126.85** | — |
| 0.92 | 1.1815 | 34,575.53 | −1.57% |
| 0.95 | 1.1080 | 33,767.02 | −3.87% |

| PMAX（kW，η=0.90） | 购电费（元） | 较 5000 |
|---|---|---|
| 4000 | 35,247.27 | +0.34% |
| **5000（题面）** | **35,126.85** | — |
| 6000 | 35,083.29 | −0.12% |

结论两句：①效率每变动 0.01，成本约变动 0.5%，且阈值 1/η² 随之平移——与 §9 阈值判据定量一致；②PMAX 放宽到 6000 仅降 0.12%，说明 5000 kW 上限虽在 19 个时段顶格，但**不是**成本的主要瓶颈（真正的约束是早/晚峰的负载-时段结构），这也解释了注记 5 中口径选择的实际影响很小。

### 10.2 规则式"谷充峰放"对照（`p1_greedy.py`）

按文献常见做法构造规则式基线（R1 光伏先直供后充盈余；R2 λ≤q20=0.4333 电网谷充；R3 λ≥q80=1.006 峰放；R4 其余直购；R5 SOC 感知的闭环修正至 6000）。结果：

| 策略 | 购电费（元） | 全天购电量 (kWh) |
|---|---|---|
| 规则式贪心 | 39,241.62 | 61,782.1 |
| **LP 最优** | **35,126.85** | **59,482.7** |
| 差距 | **+4,114.77（+11.71%）** | +2,299.4 |

差距的两个机理（论文可用作"为什么需要全局优化"的论证）：①**阈值粗糙**：q80=1.006 漏掉早峰 0.69-1.00 的放电机会，该段改用 0.69-1.00 的高价直购，而 LP 依据 γ=0.532（有效成本 0.591）全部用储能覆盖；②**无前瞻**：贪心把罐早早充满，既挤占 5:40 全日最低价段（0.371）的利用价值，又使午后高价光伏盈余无处可充——LP 通过"存什么、何时存、何时用"的全局配对避免这两类损失。

### 10.3 与赛题文献的方法定位

两篇赛题文献：袁龙篇用动态规划+SOC 离散化（24 段小时级，存在离散化误差且丢失 10 分钟结构），并有投资回收年限的参数讨论；王先齐篇用固定调度策略+NSGA-II 多目标优化，并为设备状态引入 0-1 变量。P1 的定位：**完全信息确定性场景下，LP 给出可证明的精确最优（无离散化误差），KKT 对偶给出机制解释（无需 0-1 变量），并以此量化规则式/启发式方法的最优性差距**（+11.71% 即为文献路线在本题的代价）。

## 参考文献（P1 相关）

[1] Sioshansi R, Denholm P, Jenkin T, Weiss J. Estimating the value of electricity storage in PJM: Arbitrage and some welfare effects[J]. Energy Economics, 2009, 31(2): 269-277.（已下载：`C题/paper/refs/Sioshansi_2009_PJM_storage_arbitrage.pdf`）
[2] Salles M B C, et al. Potential arbitrage revenue of energy storage systems in PJM[J]. Energies, 2017, 10(9): 1456.（MDPI 开放获取，自动下载被拦，待手动下载）
[3] Wang H, Zhang B. Energy storage arbitrage in real-time markets via reinforcement learning[C]. IEEE PES General Meeting, 2018.
[4] Klosterman S. Designing an energy arbitrage strategy with linear programming[EB/OL]. steveklosterman.com（LP 标准做法的教学性表述与文献汇总）.
[5] 王凌云, 王晓敏, 胡兴媛, 等. 基于多智能体微网的需求侧分时电价优化调度[J]. 信息与控制, 2020, 49(4): 429-435.（已下载：refs/王凌云_2020…pdf）
[6] 宋名扬, 刘媛, 程海青. 基于分时电价的并网型微电网优化调度方法研究[J]. 智能电网, 2026, 16(3): 80-85. DOI: 10.12677/sg.2026.163009.（已下载：refs/宋名扬_2026…pdf）
[7] 王先齐, 吕智林, 汤泽琦. 基于分时电价机制的并网型微网多目标动态优化调度[J]. 电力系统保护与控制, 2017, 45(4). DOI: 10.7667/PSPC160250.（赛题文献，`C题/paper/`）
[8] 袁龙. 分布式光伏发电与储能协同调度模型研究[D].（赛题文献，`C题/paper/`；扫描件，公式引用需页面图像）
[9] Boyd S, Vandenberghe L. Convex Optimization[M]. Cambridge University Press, 2004.（§5.5 LP 对偶、§5.3 KKT 条件）
[10] Schweppe F C, Caramanis M C, Tabors R D, Bohn R E. Spot Pricing of Electricity[M]. Kluwer Academic Publishers, 1988.（边际电价/实时定价思想源头）
